# Motia Homebrew Tap

Install Motia CLI via Homebrew:

```bash
brew tap MotiaDev/tap
brew install motia-cli
```

## Formulas

- **motia-cli** - CLI for scaffolding Motia projects (Node.js, Python, or Mixed)
